<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.desafio.android.Application"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <activity
            android:name="com.desafio.android.presentation.demonstration.presentation.DemonstrationActivity"
            android:exported="false"
            android:label="@string/title_activity_demonstration"
            android:theme="@style/AppTheme" />

        <activity
            android:name="com.desafio.android.presentation.logs.presentation.LogsActivity"
            android:exported="false"
            android:label="@string/title_activity_demonstration"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.desafio.android.presentation.login.presentation.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>